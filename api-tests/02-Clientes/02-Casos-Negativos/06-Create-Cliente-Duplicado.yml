info:
  name: 06-Create-Cliente-Duplicado
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/clients"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Cliente API Bruno Prueba",
        "cuit": "20-12345678-6",
        "email": "",
        "phone": "",
        "whatsapp": "",
        "cbu": null,
        "collection_contact": "",
        "collection_email": "",
        "collection_phone": "",
        "collection_portal_password": "",
        "collection_portal_url": "",
        "collection_portal_username": "",
        "estado": "Activo",
        "serie": "",
        "startdate": "2026-09-02",
        "tax": "",
        "taxes": [],
        "zone": null,
        "zone_name": null
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe rechazar un cliente con mismo nombre y CUIT (duplicado)", function () {
            expect(res.status).to.equal(422);
        });

        test("Debe retornar un error de validación", function () {
            expect(res.body).to.have.property("errors");
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "422"
      description: La creación de un cliente con el mismo nombre y CUIT de un cliente existente debe ser rechazada con código 422
    - expression: res.body
      operator: isJson
      description: El body de la respuesta debe ser un JSON válido

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
