info:
  name: 04-Delete-Cliente
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/clients/{{sellerClienteId}}"
  headers:
    - name: Authorization
      value: Bearer {{sellerToken}}
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe rechazar la eliminación del cliente", function () {
            expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
